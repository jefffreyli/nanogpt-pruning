# Quickstart

Create and active a python virtual environment.
```
python3 -m venv venv
source venv/bin/activate
```

Install all dependencies.
```
pip install -r requirements.txt
```

Run main.py for sanity check.
```
python experiment/main.py
```
